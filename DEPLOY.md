# GitHub Pages 部署步驟

1. 打開你的 repository：
   https://github.com/cherry805559-dev/deployable-website

2. 點 `Add file` -> `Upload files`。

3. 上傳這個資料夾裡的檔案：
   - index.html
   - README.md

4. Commit message 可填：
   更新品牌故事文案為手作的溫度

5. 到 `Settings` -> `Pages`。

6. Source 選 `Deploy from a branch`，Branch 選 `main`，資料夾選 `/root`，按 `Save`。

7. 發佈後網址通常會是：
   https://cherry805559-dev.github.io/deployable-website/

備註：如果你要讓 https://www.jinding.tw/ 指到這個 GitHub Pages，需要另外設定網域 DNS 與 GitHub Pages 的 Custom domain。
